## SpMV using Eigen

Sparse matrix-vector (SpMV) using Eigen library.

### Requirements: you must first download the Sparse Suite matrices used in the test.

#### Compile
```bash
make compile
```

#### Run

```bash
make run
```
