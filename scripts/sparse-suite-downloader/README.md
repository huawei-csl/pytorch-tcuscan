## Download sparse suite matrices

```bash
export SPARSE_SUITE_HOME=/scratch/TCUSCAN/sparse-suite-matrices/ssgetpy-downloaded-matrices/
make
```

It will download the sparse suite matrices under `~/.ssgetpy/` and output a file `sparse_suite_matrices.csv` containing all downlaoded matrices.
